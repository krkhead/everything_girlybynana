# Neon Postgres — get from Vercel Marketplace (Settings > Integrations > Neon)
DATABASE_URL=

# NextAuth — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Admin credentials — run: npx tsx scripts/seed-admin.ts
ADMIN_EMAIL=najegs24@gmail.com
ADMIN_PASSWORD_HASH=

# Vercel Blob — get from Vercel Dashboard (Storage > Blob > Create Store)
BLOB_READ_WRITE_TOKEN=
